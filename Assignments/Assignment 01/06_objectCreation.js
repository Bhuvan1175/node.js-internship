/**
 *
 *  @file Question 06
 *  @description
 *   Create an object called person with properties name (a string),
 *   age (a number), and isStudent (a boolean).
 *   Print each property of the object to the console.
 */

// creating Object

const person = {
  name: "bhuvi",
  age: 21,
  isStudent: true,
};
console.log(person["name"]);
console.log(person["age"]);
console.log(person["isStudent"]);
