/**
 *  Bhuvaneshwar Bagde
 *  @file Question 03
 *  @description
 *  Create two boolean variables, isTrue and isFalse, with the
 *  values true and false.
 *  Use logical operators (&&, ||, !) to print the results of 
 *  different combinations of these variables.
 *
 */

// Creating Boolean Variable

let isTrue = true;
let isFalse = false;

// using Logical Operators
console.log(
  `And Opearion   of ${isTrue} and ${isFalse} : ${isTrue && isFalse}`
);
console.log(
  `Or  Operation  of ${isTrue} or  ${isFalse} : ${isTrue || isFalse}`
);
console.log(
  `Not Operation  of ${isTrue} !=  ${isFalse} : ${isTrue != isFalse}`
);
